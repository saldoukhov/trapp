/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExistingTranslationsPipe } from './existing-translations.pipe';

describe('Pipe: ExistingTranslations', () => {
  it('create an instance', () => {
    let pipe = new ExistingTranslationsPipe();
    expect(pipe).toBeTruthy();
  });
});
