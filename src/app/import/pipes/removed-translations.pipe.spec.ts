/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RemovedTranslationsPipe } from './removed-translations.pipe';

describe('Pipe: RemovedTranslations', () => {
  it('create an instance', () => {
    let pipe = new RemovedTranslationsPipe();
    expect(pipe).toBeTruthy();
  });
});
